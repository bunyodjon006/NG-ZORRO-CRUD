export interface TeachersResponse {
    // postId:number;
    name:string;
    age:string;
    addrees:string;
    key:string; 
}
export interface TeachersRequest {
// postId:number;
     name:string;
     age:string;
     addrees:string;
     key:string;
}